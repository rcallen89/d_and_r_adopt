<main class="pets-sec">
  <h2>List of Pets at <%= link_to @shelter.name, "/shelters/#{@shelter.id}" %> </h2>
  <%= link_to "Create Pet", "/shelters/#{@shelter.id}/pets/new" %>
  <section class="pets-sec">
    <% @shelter.pets.each do |pet| %>
      <article class="pets-<%= pet.id %> pets">
        <h3><%= link_to pet.name, "/pets/#{pet.id}" %></h3>
        <%= image_tag(pet.image, size: "120") %>
        <p>Age: <%= pet.approximate_age %></p>
        <p>Sex: <%= pet.sex %></p>

        <%= link_to "Update Pet", "/pets/#{pet.id}/edit"%>
        <%= link_to "Delete Pet", "/pets/#{pet.id}", method: :delete %>
      </article>
    <% end %>
  </section>
</main>
